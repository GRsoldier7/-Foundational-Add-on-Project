# Local Host Operations

This directory is intentionally outside the portable Foundation AddOn surface.

Files here are Aaron-local workstation or hardware operations that should not be treated as
shared project bootstrap logic, portable skill infrastructure, or reusable repo scripts.

If a script in this directory becomes generally useful, move it back into `scripts/` only after
all hardware, host-path, and workstation assumptions have been removed.
